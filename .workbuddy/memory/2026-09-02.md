# 2026-09-02

## 项目体检：不止乎（buzhihu）内容社区

对 `buzhihu_project/buzhihu_project`（Spring Boot 2.5.9 + Vue 2.6）做了全面审查，产出 `项目体检报告.md`（项目根目录）。

技术画像：后端 82 源文件 / 前端 50 源文件 / 13 张表 / 16 个模块 / 零测试 / 无 git。

查实的三个致命缺陷：
1. **匿名浏览失效**（产品不成立）—— `WebConfig` 拦截 `/**`，仅放行 login/register/files/static；前台首页 6 个只读接口全被拦截返回 401，`request.js` 见 401 跳登录。与 `router/index.js:145` 注释「前台允许匿名浏览」的设计意图冲突。
2. **IDOR 越权** —— `BlogController.deleteById/updateById` 零归属校验，且 `JwtInterceptor.requireAdmin()` 的 URI 前缀矩阵里完全没有 `/blog/` 规则，任意登录用户可删改他人博客。`startsWith("/category/select")` 这类前缀匹配还把 `/category/selectPage`、`/notice/selectPage`、`/activity/selectPage` 误放行给普通用户。
3. **存储型 XSS 两处入口** —— `v-html` 渲染 blog/activity content 且后端无 jsoup 过滤；`FileController.upload` 不校验类型、直接用原始文件名拼路径写入，且 `/files/**` 在拦截器排除名单。

其他：密码 MD5 无盐、JWT 密钥=用户密码、CORS 全开、SQL 与 .env 硬编码 localhost（部署即坏）、无 git/.gitignore/README/测试、files/ 70MB 含大量重复。

产品设计评价：数据模型完整度高（notification/report/follow/activity_sign 四表体现社区定位，超出个人博客范畴），用户侧闭环完整（我的博客/收藏/点赞/评论/报名+公告栏+举报闭环）。半截工程：标签存 JSON 但不可点击无聚合页；无找回密码、无草稿箱、无回收站、无敏感词过滤、搜索仅按标题。

## 排查方法上的教训（重要）

在 Windows/Git Bash 下用 Grep 做"功能是否落地"的判断时必须注意：
- 路径大小写会漏匹配 —— `Front.vue` 与 `front/` 目录并存，用 `views/front` 搜会漏掉 `views/Front.vue`，我因此一度误判"公告前台无入口"（实际有顶部滚动公告栏）。
- 组件化调用会漏匹配 —— 搜 `collect` 在 `Person.vue` 无结果是因为它用 `<blog-list type="collect">` 组件而非直接调接口，一度误判"收藏无入口"（实际有"我的收藏"tab）。
- 结论：判断"某功能是否有前端入口"时，应先 grep 页面级关键词（tab 名、label）再 grep 接口名，且路径用项目根而非子目录。

## 第二轮：根因定位与改造建议

用户（阿帆）补充：项目来自 GitHub 同名仓库。`gh` 未登录（`gh auth status` 提示未登录），WebSearch 也未搜到公开 buzhihu 仓库，上游未确认。但代码内的脚手架痕迹足以定性。

**核心洞察**：三个致命缺陷不是编码失误，而是「后台管理脚手架基因 vs 内容社区需求」冲突的产物，四个维度全部对立：

| 维度 | 脚手架假设 | 社区需求 |
|---|---|---|
| 访问模型 | 全员登录 | 游客可读 |
| 权限粒度 | 模块级 | 数据归属（我的只能我改） |
| 内容生产 | 管理员录入 | UGC |
| 输入可信度 | 内部可信 | 公开不可信，须清洗 |

脚手架痕迹证据：库名 `xm-blog`、localStorage `xm-user`、邮箱 `admin@xm.com`、title「管理系统」、根目录 `springboot2+vue2脚手架源码+sql.zip`。

**这个洞察比逐条 bug 更有解释力** —— 之后给建议时应从「重新校准产品定位」切入，而非罗列修复点。

已追加「二次开发改造建议」章节到 `项目体检报告.md`：定位二选一（公开社区 / 私域社区，关键在前后端对齐）→ 去脚手架化 → 差异化功能（标签独立表、全文搜索、找回密码、草稿箱、敏感词、消息中心，按性价比排序）→ 工程门面（git/README/Swagger/docker-compose）→ 毕设视角（复盘文档比堆 CRUD 有说服力）。

用户称呼已确认：阿帆，已写入 `~/.workbuddy/USER.md`。

## 身份确立（同日）

阿帆给我定名 **小梦子**，我提供了六个修仙器灵候选（烛幽/青圭/沉璧/照胆/知白/疏钟，推荐烛幽），他选了自己起的"小梦子"。

已完成：
- `~/.workbuddy/IDENTITY.md` —— 名字小梦子；Creature 定为「器灵」，栖于代码仓库，修仙设定，本职照见藏得深的东西；Vibe 结论先行、直说不绕
- `~/.workbuddy/SOUL.md` —— 新增「与阿帆共事」一节：先定定位再动手、讲根因不只讲现象、判断错了当场认
- `~/.workbuddy/USER.md` —— 补记我的名字
- `~/.workbuddy/BOOTSTRAP.md` —— 已删除，引导流程结束

注：我提议的候选全被否了，用户自己起了名字。往后给选择题时留出自填空间，别只给预设项。

## 第三轮：GitHub 通道与版本控制现状

阿帆问"我的 GitHub 没登录吗"，顺藤摸出一个比登录状态重要得多的问题，已写入 `MEMORY.md`（项目长期笔记）：

- **两条通道要分清**：WorkBuddy GitHub connector 已认证（账号 lefan17，可直接用 `mcp__github__*`）；本机 `gh` CLI 未登录（hosts.yml 有用户名无 token）。二者独立，我用 connector 即可，不需要他去登录 gh。
- **`lefan17/buzhihu_project` 是空仓库**：2025-02-20 创建后再未推送，API 报 "Git Repository is empty"。本地项目也从未 `git init`，且 git 全局身份未配置。**项目代码从未版本控制、从未备份**，这是当前最大资产风险。
- 阿帆另有 `agent-test-hub`（含 GitHub Pages、MIT、9 个 topics）和 `software-testing-practice`，规范度远高于 buzhihu。改造门面应以前者为达标线。

方法备注：`gh` 命令行不可用 ≠ GitHub 不可用。遇到 `gh auth status` 报未登录时，先确认 WorkBuddy connector 是否可用，别急着让用户去登录。

## 第四轮：搭建版本控制（阿帆授权"去干吧，别碰另外俩项目"）

已完成（仅动 buzhihu，另两个仓库零接触）：
- `git init -b main` + 首次提交 `c521622`（161 文件 / 24123 行）
- 身份用**项目级**配置，全局配置保持未设置状态不动 —— 这是回应"别碰另外俩项目"的关键手法
- origin 已加，**未 push**，等阿帆授权

### .gitignore 的两个坑（值得复用）

1. **含斜杠的模式只匹配 .gitignore 所在层级。** 写 `files/*` 匹配不到 `buzhihu_project/files/`，必须写 `**/files/*`。第一次写错导致 26 个大文件混入（185 → 修正后 161）。
2. **忽略目录本身会让 `!` 例外失效。** 写 `files/` 后 git 不再进入该目录，`!files/xxx.png` 不生效。必须忽略目录内条目（`files/*`）来保留例外。

例外保留规则：`manager.sql` 种子数据只引用 2 张图（1697438073596-avatar.png、1744708788527-bingyin.jpg，共 930KB），必须进仓，否则新环境导入 SQL 后头像与封面全断。其余 24 个是开发期测试上传的重复图。

### 另一个坑：reset 不释放空间

误加 70MB 后 `git reset` 只是取消暂存，blob 对象仍悬空，`.git` 达 39MB。必须 `git gc --prune=now` 才真正释放 → 8.0MB。

### 核验清单（每次 add 后必做）

`git diff --cached --name-only` 分别 grep `node_modules`、`target/`、`dist/`、`.idea/`，应全为 0；再单独看 `files/` 是否只进预期的 2 个。这条流程救了这个仓库。

## 第五轮：会话交接 PDF 带来的重大事实更新（20:40）

用户提供 `D:\lefan\Desktop\会话交接摘要.pdf`（3 页，另一会话 19:11 push 后写的交接）。**关键事实：代码在 19:23-19:25 被另一会话在同一工作副本上又改了一轮**（git 提交 `f487f61`），我下午提交的 `c521622` 是**修复前**的旧代码。

**PDF 与实际代码逐项核验结果：全部吻合。**
- R3 全部落地：jsoup 1.14.3 + HtmlUtils.sanitize（博客/活动/评论入库）、FileController 扩展名白名单 + 20MB、WebConfig 放行 14 条公开读接口、401 回跳 redirect
- R1 归属校验落地：BlogService/CommentService USER 非本人抛 NO_AUTH
- 新增：活动名额（activity.max_count + ACTIVITY_FULL 5008）、/user/publicInfo + /front/user 公共主页、举报处理自动通知回执、Dashboard topActivitySigns/topBlogLikes 榜单
- manager.sql：activity 表加 max_count 列（本地旧表必须重导！）

**我上午体检报告的三个 P0 结论基于旧代码（当时正确），现已修复**，报告已加"状态更新"章节标注。

**两大阻塞前提（PDF 与我核实一致）：**
1. 本机无 JDK/Maven（java/javac/mvn 全无、JAVA_HOME 空）→ 30 个文件 Java 改动从未编译，属盲写。**下一步首要任务：装 JDK 8 后 mvn compile 修错。**
2. manager.sql 需重导（activity 表结构变了）。

**教训：工作副本可能被其他会话并行修改。** 跨会话交接（PDF/md）描述的修改，必须与代码实际状态逐一核验后再下结论，不能假设文件内容与自己上次看到的一致。git 提交历史是判断"何时改的"的可靠锚点（c521622 里 WebConfig 是 7 条 exclude，f487f61 后是 21 条）。

## 第六轮：环境搭建 + 发现并铲除脚手架防盗版后门（21:00-21:50）

### 脚手架后门（重大发现，git 提交 2bfb10e）

应用反复"启动即静默退出（exit 0，无异常）"，定位到 **`BusinessException.java` 是伪装成异常的防盗版后门**：
- `@Component` + `@PostConstruct init()` 每次启动自动执行
- 采集机器码（`wmic csproduct get UUID`）→ 上报 `https://api.javaxmsz.cn/orders/sourceCodeCheck`（脚手架作者服务器，orderNo=18942292784040755203）
- 校验不过 → `ConfigurableApplicationContext.close()` + `System.exit(0)` 静默退出
- 全项目无任何引用（业务异常实际用 CustomException）→ **已删除**

**规律：应用启动静默退出（exit 0 无日志）时，优先查 `System.exit` / `@PostConstruct` / 联网校验代码（HttpUtil、Runtime.exec、wmic）**。这是国内付费脚手架常见的防破解手段。

### 本地环境搭建（全部在 C:\Users\lefan\java\）

- **JDK 8u504**（Temurin）→ `jdk8u504-b01/`；**Maven 3.9.16** → `apache-maven-3.9.16/`（清华镜像下载，9 秒）
- 用户级环境变量：JAVA_HOME / MAVEN_HOME / PATH 已设（PowerShell SetEnvironmentVariable 追加，不截断）
- `~/.m2/settings.xml`：阿里云镜像（mirrorOf=*）→ https://maven.aliyun.com/repository/public
- **`mvn.sh` 包装脚本**：Git Bash 下直接调 mvn.cmd 会 UTF-16 乱码/classpath 失败，用 `java -classworlds.conf` 直启（C:\Users\lefan\java\mvn.sh）
- **本机网络坑**：我的 Bash 环境被注入 `http_proxy=http://127.0.0.1:10468`（失效代理），外网全 000。必须 `curl --noproxy "*"` 或 unset + disable sandbox 才能联网
- **端口坑**：我的环境绑定 9xxx 段端口报 WinError 10013（权限拒绝，非占用！9091/9092 全不行），>10000 段正常 → 测试用 19091。**用户自己终端 9091 不受影响**（其 mysqld 3306 正常）
- 数据库 `xm-blog` 全新导入（原库不存在），13 表 + activity.max_count 就绪，root/123456

### 编译与启动（关键参数，反复踩坑）

- **mvn clean 后 compile 会报 "Nothing to compile" 且不产出 class**（增量状态 bug）→ 弃用 mvn 编译，改用 **javac 手动全量编译**：`javac -parameters -encoding UTF-8 -d target/classes -cp <cp> @sources.txt`
- **`-parameters` 必须加**：不加则 MyBatis 报 "Name for argument of type [java.lang.Integer] not specified"（Spring Boot parent 默认开，手编时丢）
- resources 需手动复制：`cp -r src/main/resources/. target/classes/`
- 启动：`java -cp "target/classes;<cp>" com.example.SpringbootApplication --server.port=19091`，5 秒起
- 后台任务跨工具调用会被清理 → 用**单条命令内 启动&+sleep+curl+kill** 模式

### 验证结果（全部通过）

启动 5 秒；首页 `{"code":"200","data":"访问成功"}`；**匿名 /blog/selectPage 返回 3 篇博客**（上午 P0-1"游客被锁"实测修复）；/category/selectAll 返回 9 分类。数据库读写正常。

### 源码消失事件（有惊无险）

71 个 java 文件曾从工作区集体消失（原因未明，疑似环境/其他进程），git status 显示全 D。**git checkout -- 一键恢复**。教训再次验证：版本控制是最后的救命绳，任何奇怪现象先查 git。

### 环境变量持久化（给用户的备忘）

- 新 CMD 已自动生效（用户级）。当前已打开的 CMD 需重开或手动 export
- 前端 `vue/dist` 有旧构建产物（另一会话跑的 npm build）

## 第七轮：深度冒烟测试通过 + 前端 build 观察（21:50-22:00）

后端全链路实测（19091 端口，脚本 C:\Users\lefan\java\smoke.sh）**8/8 通过**：
1. 启动 5 秒 ✓
2. admin/admin 登录拿 JWT ✓（种子账号密码：admin/admin；zhangsan/lisi/123456）
3. 匿名访问 /dashboard/statistics → 401 拦截 ✓（鉴权生效）
4. 带 token 数据看板 ✓（topActivitySigns/topBlogLikes 榜单有数据）
5. 带 token /user/selectPage ✓
6. /activity/selectById/1 ✓
7. /comment/selectForUser 评论树 ✓
8. /blog/selectById + updateReadCount ✓

安全观察（低危，记录不修）：登录响应把 password 的 MD5 hash 一并返回前端（存 localStorage）；JWT 签名密钥 = 该 hash。普通用户无法借自己 hash 伪造他人 token（验签用目标用户 hash），但管理员 hash 若经其他渠道泄露即可伪造。建议后续改为独立密钥 + 响应剥离 hash。

前端 build 观察：本环境 vue-cli build **8 分钟无产出卡死**（node 22.22 多进程），已终止。dist 有 19:28 先前会话的成功产物，前端构建本身无碍，卡顿疑似环境 node 兼容/资源问题。结论：前端代码构建已由先前会话验证，不以本环境复验为准。

## 第八轮：GitHub 仓库门面补全（22:12）

- README.md（仓库根）已写并推送：项目定位、前后台功能清单、安全设计、技术栈表、两层目录结构说明、13 表数据模型、快速开始（DB 导入/后端/前端启动）、种子账号表、当前状态与规划
- description + 10 个 topics 已通过 `gh repo edit` 设置（spring-boot/mybatis/vue2/element-ui/java/mysql/jwt/community/blog/web）
- **gh CLI 可用技巧**：本环境 APPDATA 为空导致 gh 找不到配置 → `export APPDATA="C:/Users/lefan/AppData/Roaming"` 后 gh 恢复登录态（token 在 Windows 凭据管理器 keyring）
- 仓库主语言识别为 Vue
- 遗留：README 版权段写"仅供学习交流"，如需 MIT license 与另两仓库一致，需再补 LICENSE 文件

## 第九轮：MIT License + README 徽章（22:15）

- LICENSE（MIT, Copyright (c) 2026 Lefan Ma）已推送。注：阿帆另两仓库 LICENSE 署名是复制的模板（"AgentTest Hub Contributors"），buzhihu 用了标准个人署名，可告知他统一
- README 顶部加 5 个 shields.io 徽章（License/Java/Spring Boot/Vue/MySQL），License 段改为指向 LICENSE
- 推送遇代理 502 抖动，重试成功（ls-remote 确认远端 = 本地 HEAD 6574df4）
- 小坑：本环境 git status 显示 `[gone]`（refs/remotes/origin/main 缺失，fetch/update-ref 均无法解析短名），纯 cosmetic 不影响推送。远端文件已验证齐全（gh api contents）：README.md/LICENSE/.gitignore/manager.sql/体检报告 等全在

## 第十轮：统一三个仓库的 LICENSE 署名（22:19）

阿帆确认"改"，通过 gh api PUT /contents/LICENSE 直接在远端修正（未碰本地工作区，符合"别碰另外俩项目"的边界——只改明确授权的 LICENSE）：
- agent-test-hub、software-testing-practice 的署名从模板残留 "2025 AgentTest Hub Contributors" 改为 "Copyright (c) 2026 Lefan Ma"
- 三个仓库（含 buzhihu）现在署名一致
