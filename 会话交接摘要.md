# buzhihu_project 会话交接摘要（供另一对话续接）

## 项目概况
- 名称：不知乎（博客 + 活动社区，非问答），课设级
- 栈：Spring Boot 2.5.9 / Java 8 / Maven + Vue 2 / Element UI / MyBatis / MySQL
- 关键路径（注意两层目录）：
  - 工程根：C:\Users\lefan\DSH_project\buzhihu_project\buzhihu_project
  - 后端：...\springboot（端口 9091，jwt 拦截器全局）
  - 前端：...\vue（端口 8080，history 路由，baseURL=http://localhost:9091）
  - 数据库脚本：C:\Users\lefan\DSH_project\buzhihu_project\manager.sql（13 张表，须重新导入！）

## 已完成的修改（按轮次）

### R1 bug/安全修复
- manager.sql 由 2 表补全为 13 表 + 种子数据；密码一律 MD5（admin/123456；zhangsan/lisi/123456）
- 阅读数：新增 GET/PUT /activity/updateReadCount/{id}、/blog/updateReadCount/{id}；修正两个 Mapper 里 `set read_count = read_count + 1`；博客阅读数改由前端详情页触发（selectById 不再 +1）
- 取消报名：/activitySign/deleteByActivity?activityId=（按当前用户+活动定位报名记录）
- UserMapper.xml 改密码列名 bug（password = #{password}）
- Blog/ActivityMapper.xml：联表 order by 限定表名；selectComment 改 distinct+inner join（去 group by 冲突）
- CommentMapper.xml：删残留 blog_id 列；新增 deleteByRootId（评论子树删除）
- 密码 MD5 化：UserService/AdminService（注册/登录/改密）；JWT 密钥用哈希值（兼容）
- 鉴权：JwtInterceptor requireAdmin（/admin/**、user/category/notice/activity 写操作、report 处理、dashboard 等仅 ADMIN）；归属校验（博客/评论删除修改仅本人或管理员；/user/update 仅本人）；封禁用户 status='1' 拦截
- ResultCodeEnum 新增 NO_AUTH(403)、USER_BANNED(5007)、ACTIVITY_FULL(5008)
- 杂项：UserService name 默认用户名、createTime；BlogService.selectById 作者判空；.env.production 修复；路由守卫启用（前台匿名可浏览、后台需 ADMIN）；BlogList 翻页修复；NewBlog 空 blogId 防护

### R2 四个产品模块（用户要求的）
1. 站内通知：notification 表 + 实体/Mapper/Service/Controller（selectPage/unreadCount/readAll/read）；评论/回复/点赞/收藏/报名 5 个触发点接入；Front.vue 铃铛+未读角标+下拉
2. 关注+关注流：follow 表（唯一索引）+ set/isFollowing/count + /blog/selectFollow + /front/follow 页面 + BlogDetail 作者关注按钮
3. 举报+审核：report 表 + add/selectPage/handle（IGNORE/DELETE/BAN）+ 前台详情页/评论举报入口 + 后台 Report.vue（/report 菜单）+ 封禁链路（user.status，登录与拦截器双重拦截）
4. 数据看板：/dashboard/statistics（总数+7日趋势+分类分布+转化榜单）；后台 Home.vue 用纯 SVG 图表（echarts 因网络装不上）

### R3 产品设计修正（最近一轮）
- XSS：pom 加 jsoup 1.14.3；HtmlUtils.sanitize 接入博客/活动/评论入库
- 上传白名单：FileController 扩展名白名单+20MB 上限
- 401 回跳：request.js 带 redirect；Login.vue 登录后回跳
- 匿名浏览修复：WebConfig 放行公开读接口（原来前台匿名全被 JWT 拦截弹登录）
- 公共个人主页：/user/publicInfo/{id} + 新页面 /front/user + BlogList 加 userId prop + 全站作者名链接到主页
- 活动名额：activity.max_count + 报名满员校验（实时 count）+ 详情/列表"已满员"态 + 后台表单
- 举报处理自动发系统通知回执给内容作者
- Dashboard 增加"报名最多的活动/点赞最多的文章"榜单

## 验证状态
- ✅ 前端 npm run build 多次通过（vue/dist 有产物）
- ✅ 11 个 Mapper XML 标签闭合校验通过；后端 92 条路由与前端调用全部配对
- ❌ 后端从未本机编译：本机无 JDK/Maven（全盘确认无 javac、无 .m2、IDEA 2026.2.1 精简版无 jbr）；Docker CLI 有但引擎未启动
- ⏳ 已命令行启动 IDEA 打开项目（idea64.exe 运行中）

## 下一步（给新对话）
1. 首要：装 JDK 8（Corretto）或由用户提供 javac 路径 → 命令行 mvn compile 修错（改动文件较多，需编译兜底）
2. 用户需重新导入 manager.sql（13 表，密码 MD5）
3. 可选未做项：评论树形层级、title+content 全文搜索、通知聚合（"张三等 N 人赞了"）、博客草稿/审核、公告置顶、产品定位文案改名
4. 环境注意：npm/maven 外网下载不稳定（npm ECONNRESET），依赖优先本地/镜像

## 常用命令
- 前端构建：cd .../buzhihu_project/vue && npm run build
- 后端编译（装 JDK 后）：cd .../buzhihu_project/springboot && mvn -o compile（依赖已缓存时可 -o）
- IDEA 启动：start "" "C:\Program Files\JetBrains\IntelliJ IDEA 2026.2.1\bin\idea64.exe" <工程根>
