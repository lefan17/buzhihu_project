# 不止乎（buzhihu）项目体检报告

> 检查时间：2026-09-02  
> 技术栈：Spring Boot 2.5.9 + MyBatis + Vue 2.6 + Element UI  
> 代码规模：后端 82 个源文件 / 前端 50 个源文件 / 13 张数据表

---

## ⚠️ 状态更新（2026-09-02 晚，重要）

本报告主体基于**修复前**的代码（git 提交 `c521622`）。当天 19:23-19:25，另一会话在交接前完成一批修复（git 提交 `f487f61`），**正文中的三个 P0 缺陷目前已修复**，逐项对账如下：

| 原 P0 缺陷 | 当前状态 | 修复位置 |
|---|---|---|
| ① 游客无法浏览前台 | ✅ 已修复 | `WebConfig.java` 放行 14 条前台公开读接口 |
| ② 任意用户可删改他人博客 | ✅ 已修复 | `BlogService`/`CommentService` 归属校验（USER 非本人抛 NO_AUTH） |
| ③ 存储型 XSS（两处入口） | ✅ 已修复 | pom 加 jsoup 1.14.3；`HtmlUtils.sanitize` 接入博客/活动/评论入库；`FileController` 扩展名白名单 + 20MB 上限 |

另新增：活动名额上限（`activity.max_count` + 实时校验）、公共个人主页（`/user/publicInfo/{id}` + `/front/user`）、举报处理自动通知回执、Dashboard 互动榜单、401 登录回跳。

**但有两个重大前提未满足，代码可能跑不起来：**

1. **Java 改动从未编译**——本机无 JDK/Maven（已核实 `java`/`javac`/`mvn` 全无、`JAVA_HOME` 空）。全部 Java 修改是"盲写"，装 JDK 后必须先 `mvn compile` 修错。
2. **数据库必须重新导入 `manager.sql`**——`activity` 表新增了 `max_count` 列，本地若还是旧表，活动报名/名额校验相关接口会直接 SQL 报错。

其余正文的 P1/P2/P3 与"二次开发改造建议"仍然有效，作为后续迭代清单使用。

---

## 一句话结论

**骨架搭得不错，数据模型设计超出毕设平均水平，但项目停留在"能跑通的脚手架"阶段：存在 3 个致命缺陷，其中 1 个让产品不可用、1 个让任意用户可删改他人内容、1 个可导致存储型 XSS。另外完全没有工程化基建（无 git、无测试、无文档、无法部署）。**

---

## P0 致命缺陷（必须修复）

### 1. 未登录访客无法浏览前台，产品不成立

**现象**：游客打开 `/front/home` 会被强制弹回登录页，整个前台被登录墙锁死。

**根因链路**：

- `WebConfig.java:18` 拦截器注册为 `addPathPatterns("/**")`，排除名单只有 `/`、`/login`、`/register`、`/files/**`、`/error`、`/favicon.ico`、`/static/**`
- 前台首页 `Home.vue` 在 `mounted` 中调用 4 个接口：`/activity/selectTop`、`/blog/selectTop`、`/category/selectAll`、`/blog/selectPage`
- `Front.vue` 另有 `/notice/selectAll`（顶部公告栏）、`/notification/unreadCount`
- 以上接口全部被拦截，返回 `code = "401"`
- `request.js:34` 响应拦截器见 `401` 即 `router.push('/login')`

**为什么是设计缺陷而非配置疏漏**：`router/index.js:145` 明确注释「路由守卫：后台管理区域需要登录且角色为 ADMIN；**前台允许匿名浏览**」，说明作者的设计意图是游客可读。但后端仍沿用后台管理系统的默认拦截规则，意图与实现直接冲突。对内容社区而言游客可读是生命线（内容传播、SEO、转化率）。

**副作用**：token 有效期仅 2 小时（`TokenUtils.java:51`）且无刷新机制，已登录用户每 2 小时也会被踢一次。

**修复建议**：

```java
// WebConfig.java —— 把前台只读接口加入放行名单
.excludePathPatterns("/blog/select**", "/blog/selectById/**", "/blog/selectTop",
    "/blog/selectRecommend/**", "/category/select**", "/notice/select**",
    "/activity/select**", "/activity/updateReadCount/**", "/comment/select**")
```

更彻底的做法是把拦截器改为**白名单模式**：只保护后台管理接口和写操作，其余默认放行。

---

### 2. 任意登录用户可删除/修改他人博客（越权，IDOR）

**现象**：`BlogController` 的删除与修改接口不做任何归属校验。

```java
// BlogController.java:36
@DeleteMapping("/delete/{id}")
public Result deleteById(@PathVariable Integer id) {
    blogService.deleteById(id);   // 直接删，未校验 id 是否属于当前登录者
    return Result.success();
}

// BlogController.java:54
@PutMapping("/update")
public Result updateById(@RequestBody Blog blog) {
    blogService.updateById(blog); // 直接改，未校验作者
    return Result.success();
}
```

**根因**：`JwtInterceptor.requireAdmin()` 的权限矩阵用 URI 字符串前缀拼装，其中**完全没有 `/blog/` 的任何规则**，因此 `/blog/delete/{id}`、`/blog/update`、`/blog/selectPage` 对任何持有效 token 的用户开放。

**同类越权点**（同一字符串匹配的副作用）：

| 接口                     | 问题                                                     |
| ---------------------- | ------------------------------------------------------ |
| `/category/selectPage` | 被 `!uri.startsWith("/category/select")` 放行，普通用户可调管理端分页 |
| `/notice/selectPage`   | 同上                                                     |
| `/activity/selectPage` | 同上                                                     |
| `/blog/selectPage`     | 无任何规则，完全开放                                             |

**修复建议**：弃用 URI 前缀匹配，改用 Spring Security 或自定义注解（如 `@RequiresRole("ADMIN")`）；并在 Service 层强制校验 `blog.getUserId().equals(TokenUtils.getCurrentUser().getId())`，鉴权不能只落在网关层。

---

### 3. 存储型 XSS（两处入口，后端零过滤）

**入口一 · 富文本内容**：`BlogDetail.vue:18`、`ActivityDetail.vue:69` 用 `v-html` 直接渲染 `content`，内容由 wangeditor 产出后原样入库，后端全项目无 `Jsoup`/`HtmlUtils`/任何转义。

**入口二 · 文件上传**：`FileController.upload()` 不校验文件类型、后缀、大小，直接用 `file.getOriginalFilename()` 拼路径写入：

```java
FileUtil.writeBytes(file.getBytes(), filePath + flag + "-" + fileName);
```

且 `/files/**` 在拦截器排除名单中，上传物可被公开访问。上传 `.html` 即存储型 XSS。

**攻击代价**：攻击者只需注册一个普通账号，发表一篇含 `<script>` 的博客，所有浏览该博客的访客（包括管理员）都会中招，可窃取管理员 token 进而接管后台。

**修复建议**：

- 引入 `jsoup`，在入库前用白名单（`Whitelist.relaxed()`）清洗 HTML
- 文件上传增加后缀白名单（jpg/png/gif/webp）、MIME 嗅探、魔数校验，并用 UUID 重命名，禁止使用原始文件名

---

## P1 安全问题

| 项             | 位置                                  | 说明                                                                 |
| ------------- | ----------------------------------- | ------------------------------------------------------------------ |
| 密码 MD5 无盐     | `UserService:40` `SecureUtil.md5()` | 彩虹表可秒破；种子数据 admin/admin、zhangsan/123456 均为弱口令                      |
| JWT 密钥 = 用户密码 | `TokenUtils:52` `HMAC256(sign)`     | 密码泄露即可伪造任意用户 token；且用户改密码后所有设备强制下线                                 |
| CORS 全开       | `CorsConfig:19-21`                  | `allowedOrigin("*")` + `allowedMethod("*")` + `allowedHeader("*")` |
| 数据库密码明文       | `application.yml:9`                 | 应改为环境变量注入                                                          |
| 删除即物理删除       | `BlogService.deleteById`            | 无回收站，误删不可恢复                                                        |

---

## P2 部署即坏

| 项                    | 位置                   | 说明                                                    |
| -------------------- | -------------------- | ----------------------------------------------------- |
| 图片 URL 硬编码 localhost | `manager.sql` 全文     | 所有头像/封面写死 `http://localhost:9091/files/...`，部署后全部 404 |
| 生产环境指向本地             | `.env.production`    | `VUE_APP_BASEURL='http://localhost:9091'`，生产构建仍打到本地   |
| 页面标题未改               | `vue.config.js`      | 仍是 `title = "管理系统"`                                   |
| 数据库名不一致              | `application.yml:10` | 库名 `xm-blog`，与项目 `buzhihu` 不匹配                        |

> 前两项意味着当前打包产物在任何非本机环境都是废的。

---

## P3 工程化缺失

- **无 git 仓库** —— 项目未纳入任何版本控制
- **无 .gitignore** —— `node_modules`（1.9 万+ js 文件）、`target`、`.idea` 会全部进库
- **零测试** —— `springboot/src` 下只有 `main`，无 `test` 目录
- **无 README / 无文档** —— 项目全库找不到一个 `.md` 文件
- **无 API 文档** —— 未集成 Swagger/OpenAPI
- **`files/` 目录 70MB** —— 26 个文件中大量重复：「世界尽头」5 份、「完美世界」4 份、「森林湖泊」2 份，直接堆在代码库
- **根目录杂物** —— `font_4875418_ky40si5tfxd/` 图标包、两个未清理的 zip（脚手架源码、图片素材）
- **`manager.sql` 散落根目录** —— 文件名与项目无关，建议移入 `docs/` 或 `springboot/src/main/resources/`
- **无 Docker / 部署脚本 / CI 配置**

---

## 产品设计评价

### 做得好的部分（值得保留）

- **数据模型完整度高**：13 张表覆盖内容（博客/分类/标签）、互动（点赞/收藏/评论）、社交（关注/通知）、运营（活动/报名/公告/举报）、治理（封禁）。其中 `notification`、`report`、`follow`、`activity_sign` 是"社区"而非"个人博客"的标志性表，说明设计目标是内容社区，方向正确
- **用户侧闭环完整**：`Person.vue` 提供「我的博客 / 我报名的活动 / 我的点赞 / 我的收藏 / 我的评论」五个 tab，且我的博客支持编辑与删除
- **举报闭环完整**：用户举报 → 后台 `Report.vue` 处理
- **公告落地**：`Front.vue` 顶部滚动公告栏
- **前后台按角色分流**、分页统一使用 PageHelper、管理端有数据看板

### 不合理 / 未完成的部分

| 问题            | 说明                                                                                 |
| ------------- | ---------------------------------------------------------------------------------- |
| **标签系统是半截工程** | `tags` 字段以 JSON 字符串存储，能写、能展示，但**不可点击、无标签聚合页、后端无按标签查询**。标签成了死数据，且 JSON 存储无法建索引、无法统计 |
| **游客不可读**     | 见 P0-1，与社区产品定位根本冲突                                                                 |
| **无找回密码**     | 用户忘记密码只能联系管理员，自助路径为零                                                               |
| **无草稿箱**      | 点击发布即公开，无草稿、无定时发布                                                                  |
| **无内容审核**     | 有事后举报，无事前敏感词过滤，UGC 社区风险敞口大                                                         |
| **无回收站**      | 误删即永久丢失                                                                            |
| **关注为单向关系**   | `follow` 表只有 (user_id, follow_id)，无粉丝列表、无互关概念，社交粘性弱                                |
| **无独立消息中心**   | 通知只在顶部下拉展示 10 条，点开即完                                                               |
| **搜索能力弱**     | 仅按 `title` 模糊匹配，正文不参与搜索，无高亮                                                        |

---

## 修复优先级清单

**第一批（让产品可用）**

- [ ] WebConfig 放行前台只读接口，改为白名单模式
- [ ] BlogController delete/update 增加作者归属校验
- [ ] 引入 jsoup 清洗富文本 HTML
- [ ] 文件上传增加类型白名单 + UUID 重命名

**第二批（让产品能上线）**

- [ ] 将图片 URL 改为配置化（数据库存相对路径，前端拼接 baseURL）
- [ ] `.env.production` 改为真实域名
- [ ] 修改页面 title
- [ ] 密码加盐（BCrypt），替换种子弱口令
- [ ] JWT 密钥改为独立配置项，脱离用户密码

**第三批（让项目可维护）**

- [ ] `git init` + 编写 `.gitignore`
- [ ] 补 README（启动方式、目录说明、接口约定）
- [ ] 清理 `files/` 重复文件（可省约 30MB），迁移到对象存储或独立目录
- [ ] 清理根目录 zip 与 font 包，`manager.sql` 移入 `docs/`
- [ ] 补充关键路径的单元测试
- [ ] 集成 Knife4j/Swagger

**第四批（产品打磨）**

- [ ] 标签改为独立表 + 关联表，支持点击筛选与聚合页
- [ ] 找回密码（邮箱/手机验证码）
- [ ] 草稿箱
- [ ] 独立消息中心页
- [ ] 敏感词过滤
- [ ] 回收站（软删除）

---

# 二次开发改造建议

## 核心判断

本项目基于某个 SpringBoot2+Vue2 **后台管理脚手架**二次开发（证据：库名 `xm-blog`、localStorage 键 `xm-user`、管理员邮箱 `admin@xm.com`、页面 title「管理系统」、根目录留有脚手架源码 zip）。

上文列出的三个致命缺陷不是编码失误，而是**两套设计基因冲突**的产物：

| 维度 | 脚手架原有假设（后台管理） | 不止乎的需求（内容社区） |
|---|---|---|
| 访问模型 | 所有人必须先登录 | 游客可读，内容要被看见 |
| 权限粒度 | 模块级，管理员一统到底 | 数据归属，我的内容只有我能改 |
| 内容生产 | 管理员在后台录入 | UGC，用户既是读者也是生产者 |
| 输入可信度 | 内部系统，无需清洗 | 公开不可信，必须过滤校验 |

**改造的本质，是把一个后台管理系统的骨架，重新校准为一个内容社区。**

## 第一步：先把定位定了（不要两边都想要）

当前状态是「前端路由说允许匿名，后端拦截器说必须登录」，这是定位未定导致的撕裂。二选一：

**方案 A · 公开社区（推荐）**
- 游客可读博客、活动、评论、公告；写操作才要求登录
- 拦截器改为白名单模式：默认放行，只保护写操作与后台接口
- 价值：内容可传播、可被搜索引擎收录，符合「社区」定位

**方案 B · 私域 / 内部社区**
- 全员登录，游客只看到登录页
- 后端保持现状，但把 `router/index.js` 中的匿名放行逻辑和注释一起删掉
- 价值：改造量小，适合企业内部、班级内部使用

> 关键是**前后端必须对齐**。现在的半吊子状态两头不讨好。

## 第二步：去脚手架化（决定这个项目像不像自己的作品）

| 项 | 现状 | 目标 |
|---|---|---|
| 数据库名 | `xm-blog` | `buzhihu` |
| localStorage 键 | `xm-user` | `buzhihu-user` |
| 邮箱域名 | `admin@xm.com` | 自有域名 |
| 页面标题 | 「管理系统」 | 「不止乎」 |
| 包名 | `com.example` | `com.buzhihu`（改动大但值得） |
| 目录命名 | `springboot/` `vue/` | `buzhihu-server/` `buzhihu-web/` |
| 根目录杂物 | 脚手架 zip、font 包、manager.sql | 清理 zip 与 font 包，SQL 移入 `docs/` |

> 这一步工作量约半天，但对「这是不是你的项目」的观感影响最大。

## 第三步：做出差异化（脚手架没有的东西）

脚手架给了 CRUD 骨架，以下功能才真正体现个人工作量，按性价比排序：

1. **标签系统做完整** —— 当前 `tags` 是 JSON 字符串，不可点击、无聚合页。改为 `tag` 表 + `blog_tag` 关联表，实现标签云、点击筛选、标签下文章数统计。最能体现数据库设计能力，难度适中。
2. **全文搜索** —— 当前只按标题模糊匹配。先做 MySQL 全文索引（`FULLTEXT` + `MATCH AGAINST`），进阶上 Elasticsearch，配合搜索结果高亮。
3. **找回密码** —— 邮箱验证码 + Redis 存验证码，顺带把验证码体系建起来（后续注册、改绑都可复用）。
4. **草稿箱与定时发布** —— 博客表加 `status`（草稿/已发布/定时）与 `publish_time` 字段。
5. **敏感词过滤** —— 前缀树（Trie）实现，或接入第三方审核 API。UGC 社区必备件。
6. **消息中心页** —— 当前通知只在顶部下拉展示 10 条，做成独立页面 + 已读未读筛选 + 分类（点赞/评论/关注/系统）。

## 第四步：让它像个正经开源项目

- `git init` + 编写 `.gitignore`（务必排除 `node_modules`、`target`、`.idea`、`files/`）
- **README 是门面**，至少包含：项目简介、技术栈、功能截图（前后台各 3 张）、本地启动步骤、目录结构、已知问题
- 集成 Knife4j / Swagger 自动生成接口文档
- 补关键路径单元测试：登录鉴权、博客归属校验、标签统计
- 加一个 `docker-compose.yml`（MySQL + 后端 + 前端），一键启动
- 清理 `files/` 重复图片（约可省 30MB），或整体迁移到对象存储

## 如果要作为毕设 / 面试作品

评委和面试官在意的不是 CRUD 有多少张表，而是：

- **能不能说清为什么这么设计** —— 比如关注关系为何用单向表而非互关表、通知为何独立建表而非轮询
- **有没有踩过并解决过真问题** —— 越权怎么发现的、XSS 怎么防御的、游客可读怎么实现的
- **边界情况处理** —— 并发点赞如何保证幂等、删博客时关联数据如何清理

建议把本次体检发现并修复的三个缺陷（匿名访问、越权、XSS）**写成复盘文档**：问题现象 → 定位过程 → 修复方案 → 验证方式。这比再堆十个 CRUD 模块有说服力得多。
