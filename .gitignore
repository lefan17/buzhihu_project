# ============ 依赖 ============
node_modules/

# ============ 构建产物 ============
dist/
target/
build/

# ============ IDE 与编辑器 ============
.idea/
*.iml
*.ipr
*.iws
.vscode/

# ============ 系统与日志 ============
.DS_Store
Thumbs.db
*.log
logs/

# ============ 运行时上传目录 ============
# files/ 当前约 70MB，其中绝大多数是开发期间测试上传的重复图片
# （如「世界尽头」5 份、「完美世界」4 份），不应进入版本库。
# 建议后续迁移到对象存储，或清理重复后单独托管。
#
# 注意两处写法，都踩过坑：
#   1. 必须写 `**/files/*` 而非 `files/*`。含斜杠的模式只匹配 .gitignore
#      所在目录的层级，而实际路径是 buzhihu_project/files/。
#   2. 必须忽略目录内的条目（files/*）而非目录本身（files/）。
#      一旦整个目录被忽略，git 不再进入该目录，`!` 例外规则就会失效。
**/files/*

# 例外：manager.sql 种子数据直接引用了下面两张图，
# 若一并忽略，新环境导入 SQL 后头像与文章封面会全部断裂。
!**/files/1697438073596-avatar.png
!**/files/1744708788527-bingyin.jpg
